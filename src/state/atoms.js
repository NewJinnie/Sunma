import { atom } from 'jotai';

export const isWelcomeAtom = atom(false);

export const authUserAtom = atom();

export const isSpAtom = atom();
export const isTabletAtom = atom();

export const editIsSide = atom(true);
export const editorFilesAtom = atom([]);
